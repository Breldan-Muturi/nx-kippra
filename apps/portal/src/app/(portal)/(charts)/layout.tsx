const ReportsLayout = ({ children }: { children: React.ReactNode }) => (
  <main className="flex justify-center px-2 py-4 overflow-x-hidden sm:px-6">
    {children}
  </main>
);

export default ReportsLayout;
